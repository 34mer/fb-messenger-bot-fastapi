services:
  - type: web
    name: fb-messenger-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: VERIFY_TOKEN
        value: my-super-secret-verify   # set a better secret later
      - key: PAGE_ACCESS_TOKEN
        sync: 7c0fa9617d69dfe13b10936ece41f998        # we’ll paste the real token in Render after deploy
